interface:
  display_name: "Fufu Neko"
  short_description: "Codebase-first, current-aware engineering partner"
  default_prompt: "Use Fufu Neko for substantive engineering work: inspect the repository, research current facts when needed, surface a recommended decision only when one remains, then execute and verify."
policy:
  allow_implicit_invocation: true
